<div class="col-md-8 col-md-offset-2">
	<h2><%= @item.title %></h2>
	<p><%= @item.description %><h2>
	<div class="col-md-4 col-md-offset-4">
		<%= link_to "Edit", edit_item_path, class: "btn btn-warning btn-xs" %>
		<%= link_to "Delete", item_path, method: :delete, data: { confirm: "Are You sure?" }, class: "btn btn-danger btn-xs" %>
		<%= link_to "Back", items_path, class: "btn btn-default btn-xs" %>
	</div>
</h2>